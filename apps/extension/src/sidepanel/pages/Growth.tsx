import { EmptyState } from '../../ui/index.js';

/** Placeholder until 1.6 ports `GrowthTab` (`Dashboard.tsx:1219-1380`) unchanged. */
export const Growth = () => (
  <EmptyState
    icon="📈"
    title="Followers, and where they came from"
    body="Your follower count over time, and which of the people I watch actually earned them."
  />
);
